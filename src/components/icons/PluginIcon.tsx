import { FC } from 'react';
import { IconBaseProps } from 'react-icons';
import { FaSave } from 'react-icons/fa';

export const PluginIcon: FC<IconBaseProps> = (props) => <FaSave color="#A6DCED" {...props} />;
